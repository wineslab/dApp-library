"""Runnable dApp examples, shipped in the wheel so they can be launched with
``python -m examples.spectrum_dapp`` / ``python -m examples.simple_dapp`` from an
installed ``dapps`` package (e.g. the OAI rfsim CI driving the real dApp)."""
